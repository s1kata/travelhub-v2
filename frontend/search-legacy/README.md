# Search UI — legacy snapshot (rollback)

Замороженная копия coral-поисковика (на случай эксперимента `?search=v2`).

**Сейчас по умолчанию** главная уже на исходном UI (`TH_SEARCH_UI=legacy`).

## Содержимое

| Путь | Что |
|------|-----|
| `css/` / `js/` | Снапшот wizard + coral |
| `wizard-markup.snapshot.php` | HTML `#tour-search-section` |
| `results-markup.snapshot.php` | HTML результатов (фрагмент) |

Эксперимент v2: `?search=v2` или `TH_SEARCH_UI=v2`.
